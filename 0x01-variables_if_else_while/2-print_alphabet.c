#include <stdio.h>
#include <stdlib.h>
#include <time.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */

int main (void)
{
	char i;

	putchar (i = 'a'); i++;'\n');
		putchar(i);
		putchar('\n');

	return(0);
}
